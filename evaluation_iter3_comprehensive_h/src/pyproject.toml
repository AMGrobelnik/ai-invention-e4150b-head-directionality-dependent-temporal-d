[project]
name = "eval-id5-it3-opus"
version = "0.1.0"
requires-python = ">=3.10,<3.12"
dependencies = [
    "loguru==0.7.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
